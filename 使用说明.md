# 公务员备考个人工作台

本地验收版本已完成，现已获准准备 GitHub 发布。实际发布结果以仓库 Actions 和部署页面为准。

## 立即打开

本机地址：<http://localhost:5178/>

同一 Wi-Fi 下手机地址：以开发服务启动窗口显示的 Network 地址为准。

手机预览需要电脑上的开发服务保持运行；更换网络后需重新查看 Network 地址。如果手机连接不上，请确认同网段、关闭手机蜂窝网络干扰，检查 Windows 防火墙是否允许 Node 的私人网络连接。没有自动更改防火墙。

**本地模式每个浏览器独立保存，不会同步。只有连接同一个 Supabase 账号后，才能验收跨设备同步。** 本地数据在 IndexedDB，图片以 Blob 保存。清理站点数据或使用无痕窗口可能使本地记录丢失。测试脚本在独立临时浏览器中运行，不会往你日常浏览器填入示例数据。

## 启动和构建

本电脑已经安装依赖。双击项目内 `启动工作台.cmd` 即可启动并打开页面。若开发服务已在运行，直接打开地址即可。

也可以在项目目录打开终端：

```powershell
npm run dev
```

首次在其他电脑运行需要 Node.js 22.12 或更新版本：

```powershell
npm ci
npm run dev
```

若 npm 缓存目录不可写，可指定一个有写入权限的目录，例如 `npm ci --cache .npm-cache`。

生成可作为静态网站托管的文件（只构建，不部署）：

```powershell
npm run build
npm run preview
```

构建产物在 `dist/`，预览地址为 <http://localhost:4178/>。使用相对资源路径与 hash 路由，未来放在 GitHub Pages 仓库子路径下无需 Node 常驻服务器。仓库已提供 GitHub Actions 发布流程，配置步骤见 README。

## 项目结构

```text
study-workbench/
  index.html                 网页入口
  src/
    app.js                   启动、导航与交互协调
    ui.js                    各页面与弹窗展示辅助
    styles.css               桌面、移动端与深色样式
    core.js                  日期、统计、模板生成、计时纯逻辑
    store.js                 状态与业务操作
    images.js                图片校验与缩略图
    data/local.js            IndexedDB 本地数据接口
    data/supabase.js         Supabase Auth / Database / Storage
  supabase/schema.sql        数据表、关系、RLS、私有存储、事务函数
  public/                    PWA 清单、图标、基础离线缓存
  tests/                     业务、浏览器、数据库、云接口、生产构建测试
  test-results/              测试报告和桌面/手机截图
  docs/                      设计说明与验收报告
  .env.example               公开配置示例
  vite.config.js             相对路径静态构建配置
  package.json
  package-lock.json
  启动工作台.cmd
```

## Supabase 配置

以下步骤用于你准备验收云同步时执行。本阶段没有代你创建或修改任何线上项目。

1. 在 Supabase 控制台创建一个个人项目。进入 SQL Editor，完整执行 `supabase/schema.sql`。脚本适用于全新项目，建议只运行一次；有同名表时不要重复执行或覆盖既有表。
2. 数据库会创建 `tasks`、`daily_task_templates`、`study_materials`、`material_images`、`study_sessions`。脚本同时创建私有 `study-images` Storage bucket、各表的 owner RLS 和 Storage 路径权限。
3. 在 Auth 设置中关闭 **Allow new users to sign up**（禁止新用户注册）。在 Authentication → Users 手动创建你的个人邮箱密码账号；如果控制台提供 Auto Confirm 选项，请启用。网页不提供注册或自助找回密码入口；密码忘记时在控制台管理账号。
4. 从项目的 Connect / API Keys 区域找到 Project URL 与 **publishable key**（或 legacy **anon key**）。不要使用 `secret` 或 `service_role` key。
5. 网页打开“设置 → 配置并登录 Supabase”，填写 URL 和公开 key，再使用刚才的邮箱和密码登录。手机上填写相同配置、登录相同账号。
6. 或复制 `.env.example` 为 `.env.local`，填写 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY`，重新启动开发服务。环境变量优先于网页保存的配置。更改构建用配置后需要重新构建。公开 key 会进入静态前端，这是其预期用途，权限依靠登录与 RLS；前端不得包含任何后台密钥。
7. 登录后右上角显示“已同步 + 时间”。电脑添加任务后，手机刷新即可读取；上传截图后，另一设备刷新进入同一板块即可查看。页面重新获得焦点也会读取最新数据。

登录会保持会话并由官方 SDK 刷新；没有开放注册。所有表的查询、写入、更新、删除均按 `auth.uid() = user_id` 限制；图片必须存放在该用户 ID 目录下，使用临时签名地址读取。不要把 bucket 改成 public，也不要关闭 RLS。

参考官方文档：[密码登录](https://supabase.com/docs/reference/javascript/auth-signinwithpassword)、[数据安全](https://supabase.com/docs/guides/database/secure-data)、[Storage 权限](https://supabase.com/docs/guides/storage/security/access-control)、[图片签名地址](https://supabase.com/docs/reference/javascript/file-buckets-createsignedurl)。

## 使用规则

- 新增任务：在首页输入并按回车。点击名称或铅笔修改；勾选后留在原位，可以取消勾选。
- 排序：桌面拖拽任务；手机在任务编辑弹窗使用“上移”。
- 固定任务：设置页可新增、修改、开启/关闭、删除。每天首次打开会生成当日任务；不开网页的过去日期不会补造任务。修改模板只影响之后生成的任务。
- 当天删除固定任务后不会重复生成。顺延保留任务的来源标记，次日模板仍会生成一份新的任务，因此次日可能同时有昨天未完成项和当天固定项。
- 时长：任务的“手动补记”是手工录入部分，实际总时长为它与计时记录之和。计时支持开始、暂停、继续、结束；刷新或切换标签页仍按真实经过时间计时，不自动暂停。每个浏览器一次计一个任务，多个设备的已结束计时记录会累加。
- 正在进行的计时器只属于当前浏览器，结束后才保存到云端。结束失败会暂停并保留，可再次保存，不会生成重复的同一计时记录。
- 计时按**启动日期**统计，跨午夜整段仍计入启动日；如要分两天记录，请午夜前结束后重新开始。手动分钟跟随任务所属日期，顺延会一起移动；历史计时记录留在原记录日期。
- 删除任务不删除已完成的计时记录，但该任务手动补记不再计入任务汇总；删除前请确认。连续记录按“有完成任务或学习时长”的连续自然日计算，不把自动生成未完成任务视为打卡。
- 资料板块只有行测和申论。支持文字笔记、JPG/PNG/WebP/GIF 多图、预览移除、标签、搜索、排序、编辑、删除、原图和左右切换。每张最多 20MB；HEIC 需先转为 JPEG/PNG。缩略图最长边 640px，原图不压缩。拍照由手机浏览器支持情况决定。
- 本地数据与云端账号分离，不自动迁移，防止测试内容误入正式账号。JSON 导出包含文字和结构化记录，不含图片文件，也不是完整图片备份。
- 云模式需联网保存；失败保留输入，手动重试，不设离线上传队列。同一记录在多个设备同时修改，最后成功提交的记录覆盖先前版本；请避免两设备同时编辑同一项。
- 云端图片先上传再提交资料元数据，元数据保存为单个事务。网络响应丢失时不会主动删除可能已被引用的图片；放弃失败上传可能留下私有孤立对象，可以之后在 Supabase Storage 控制台清理。尚未实现定期垃圾清理。

## 手机与 PWA

375、390、430px 为重点适配宽度。手机使用底部导航，任务操作按钮至少 44px；图片可以新窗口查看或双指缩放。

基础 PWA 包含清单、192/512 PNG 图标、主题色和 service worker。**生产构建**在 localhost 或 HTTPS 上激活基础静态资源缓存，开发模式不注册。缓存不存储 Supabase API、登录请求或云端私有图片；云模式离线不能读取最新数据或保存。

手机的局域网 HTTP 地址可用于页面与功能预览，但一般不能完整安装 PWA 或启用 service worker。真实手机 PWA 安装需要后续获得授权后部署到 HTTPS 地址，当前没有部署。

## 你需要验收

1. 在电脑和手机添加、编辑、勾选、顺延任务，确认输入和按钮舒适。
2. 固定模板启用后查看今天，再在次日打开，确认每天独立完成状态。
3. 开始计时，锁屏或离开页面后回来，暂停并结束，确认时长符合预期。
4. 用真实课程截图多图上传、删除其中一张、保存、打开原图；搜索标题、备注、标签。
5. 配置真实 Supabase 后，用手机与电脑同一账号互相验证任务和图片；退出再登录；检查私有 bucket 和禁止注册设置。
6. 在你的真实 iPhone/Android 上检查相册、拍照、键盘弹起与图片缩放。浏览器模拟测试不等于真机验收。

## 自动化验证

测试用本机已安装的 Chrome（`channel: chrome`），UI 测试需要先启动 `npm run dev`。

```powershell
npm test
npm run test:ui
npm run test:mobile
npm run test:db
npm run test:cloud
npm run build
npm run test:production
```

`test:db` 使用本地嵌入式 PostgreSQL 验证 SQL 和 RLS；`test:cloud` 模拟 Supabase HTTP 服务验证适配代码，两者都不是实际云项目联调。完整证据和未验证项见 `docs/验收报告.md`。

真实云同步与真机体验按上述清单继续验收；发布配置见 README。
