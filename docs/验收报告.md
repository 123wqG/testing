# 本地验收报告

日期：2026-09-16。环境：Windows、Node.js 24、本机 Chrome 无头浏览器。

## 已完成

今日任务（新增/编辑/完成与取消/删除/排序/单项与批量顺延）、每日固定模板、手动时长与持久计时器、行测/申论资料、图片原图及缩略图、详情/编辑/删除/放大/左右切换、标题备注搜索、标签、时间排序、月历历史、本周与今日轻量统计、连续记录、深色模式、账号与同步状态、退出登录、基础 PWA、静态构建。

Supabase 独立适配层、密码登录与会话保持、数据库 SQL、owner RLS、私有 Storage、短时签名地址已实现。配置缺失时明确进入 IndexedDB 本地验收模式，不宣称已云同步。

## 已执行并通过

| 验证 | 结果与证据 |
| --- | --- |
| 6 项核心逻辑测试 | 日期跨年/闰日、模板独立与删除防复活、顺延统计、真实活动连续天数、暂停计时、非法时长 |
| 浏览器端到端 | 增改勾选、手动时长、计时刷新与暂停、模板删除防复活、顺延及日历、3 图预览删至 2 图、保存搜索筛选、原图切换、编辑删图后重开、深色、密钥拦截、资料删除 |
| 移动布局 | 375/390/430px 和 1440px，各核心页面无横向溢出；首页、资料、历史和深色截图已查看；深色根页面背景与文字颜色另有计算样式断言 |
| 移动触控流程 | 390px 触控模拟和局域网 HTTP：新增、勾选、编辑、计时、多图查看编辑、历史、模板开关、批量顺延通过 |
| 数据库 | PGlite PostgreSQL 中完整执行 SQL；所有者保存、跨用户读写隔离、Storage 路径隔离、伪造 owner 拒绝、资料事务回滚、匿名读取拒绝、资料删除清理关联 |
| 云接口契约 | 官方 SDK 对模拟 HTTP 服务；登录保持、两个独立浏览器刷新读取任务、上传失败不发布元数据并保留草稿、重试成功、签名图读取、离线不伪保存、退出清屏 |
| 生产构建 | `vite build` 成功，输出静态 dist，无常驻应用后端 |
| 静态子路径与 PWA | `/study-workbench/` 子目录、hash 刷新、manifest、图标、service worker 激活、断网刷新外壳、本地 IndexedDB 离线写入 |

原始结果在 `test-results/browser-report.json`、`schema-report.json`、`cloud-contract-report.json`、`production-report.json`，截图为 `today-375.png`、`today-390.png`、`today-430.png`、`today-1440.png`、`settings-dark.png`。截图数据只存在于隔离的测试浏览器中。

## 尚未实测的边界

- 用户尚未提供真实 Supabase 项目，所以未真实创建云数据库/账号、未真实上传图片，也未完成真实手机与电脑的云同步验收。没有把模拟接口测试作为真实云端成功。
- iOS/Android 相册、相机、双指缩放、锁屏计时与 PWA 安装待真机验收。手机宽度检查使用 Chrome 视口模拟。
- 没有做长期大规模图片压力测试、自动图片孤立对象回收、离线云上传队列或协同冲突合并。
- 模板按打开当天生成，不为未打开的过去日期补任务；跨午夜计时按启动日期归档；多设备同时编辑同一记录以最后成功写入为准。

## 未进行的操作

没有创建 GitHub 仓库、git commit、push、GitHub Pages 配置或任何正式线上部署。下一步为用户本地验收。
